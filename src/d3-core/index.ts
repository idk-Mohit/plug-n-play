/**
 * D3 visualization engine — core utilities only.
 *
 * Chart components live under `@/d3-core/charts/*` and are imported explicitly so
 * bundlers can keep heavy or future chart code out of the main chunk.
 */

export * from "./core";
